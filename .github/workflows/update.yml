name: 数据仓库更新流水线

on:
  workflow_dispatch:

env:
  UNITY_ASSETS_REPO: 'SeerAPI/seer-unity-assets'
  CONFIG_SOURCE_REPO: 'SeerAPI/config-sources'

jobs:
  update-unity-assets:
    runs-on: ubuntu-latest
    outputs:
      has_update: ${{ steps.update.outputs.has_update }}

    steps:
      - name: 检出脚本仓库
        uses: actions/checkout@v4
      
      - name: 设置 Python 环境
        uses: ./.github/actions/setup-environment
        with:
          python-version: '3.10'
          install-dependencies: 'true'

      - name: 检出仓库
        id: checkout-repo
        uses: ./.github/actions/checkout-repo
        with:
          repository: ${{ env.UNITY_ASSETS_REPO }}
          path: 'seer-unity-assets'
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 1

      - name: 更新 seer-unity-assets
        id: update
        working-directory: ${{ steps.checkout-repo.outputs.repo-path }}
        run: |
          echo "🚀 开始处理数据..."
          uv run ../scripts/seer_unity_assets/update.py
          echo "✅ 数据处理完成"

  update-config-sources:
    runs-on: ubuntu-latest
    needs: update-unity-assets
    if: needs.update-unity-assets.outputs.has_update == 'true' 

    steps:
      - name: 检出脚本仓库
        uses: actions/checkout@v4

      - name: 设置 Python 环境
        uses: ./.github/actions/setup-environment
        with:
          python-version: '3.10'
          install-dependencies: 'true'

      - name: 检出仓库
        id: checkout-repo
        uses: ./.github/actions/checkout-repo
        with:
          repository: ${{ env.CONFIG_SOURCE_REPO }}
          path: 'config-sources'
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 1

      - name: 运行数据处理
        working-directory: ${{ steps.checkout-repo.outputs.repo-path }}
        run: |
          echo "🚀 开始处理数据..."
          uv run ../scripts/config_sources/update.py
          echo "✅ 数据处理完成"